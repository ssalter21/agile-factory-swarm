# The acceptance criteria. Real values, never placeholders — each one is a behaviour someone can
# observe. Survives the run only as something that runs (§11).

Feature: <the behaviour, in the user's words>

  Scenario: <the case>
    Given <the starting state>
    When <the user does one thing>
    Then <the observable result>
