# Questions for you — <slug>

<!-- This is the FIRST thing the human reads at the seam, and the only file there that asks them
     for anything (§11). Answering these is what approval is, so the constraints are hard:

       - at most THREE questions. For each one past the third, say why the ladder failed for it
       - at most one screen each, about forty lines
       - every question titled on the first screen, so the whole batch is visible before any of it
         is read
       - ordinary technical English; a term this run invented is defined here or not used
       - a recommendation ONLY where a voice won the argument, reported as that. Where the voices
         were tied, offer none and say so — the absence tells the human it is theirs alone

     This file existing means the seam is open. It is deleted, by the Spec Writer on a fold, once
     every question is answered — and its absence is what makes the spec approvable. -->

<!-- Delete whichever line does not apply: -->
The spec swarm **blocked** after the <pass> pass (§5, §10). There is no spec to read yet.
The spec swarm **finished**. `spec.md` is beside this file; these are the questions it could not
settle.

**Answer in `answers.md`, in prose.** Then run `/spec-swarm` again: the spec writer folds your
answers into `spec.md` and deletes this file. Do not edit `spec.md` by hand.

Everything else the voices raised is settled or in the register at `spec.md`, each entry with its
cost if wrong. Disagreeing with one of those costs a draft, not a build.

**On this page:** 1. <title> · 2. <title> · 3. <title>

---

## 1 — <the question, as a question>

**The fork.**

```text
  <option A>  ->  <what changes down this branch>
  <option B>  ->  <what changes down this branch>
```

**Why nobody here can decide it.** <What the researcher looked for and did not find, or which two
readings of the brief are both defensible. Name the sources.>

**What being wrong costs.** <Concrete. "Two drafts diverge and one is discarded" and "the QA pass is
invalidated as well as the build" are different answers and the human is choosing between them.>

**Recommendation.** <Only where a voice won the argument: which voice, what it argued, and why the
other conceded nothing. Otherwise write: *None. The voices were tied, so this one is yours.*>

---

## 2 — <the question>

<!-- same shape -->

---

## Past the cap

<!-- Only if more than three questions survived the stake filter. One line each, saying why the
     ladder failed for it — why research could not settle it and no assumption was safe. -->
